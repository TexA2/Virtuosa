План
*viWidget
	+ вынести работу с PointCloud в одтельный класс (viManager), (пока с одним облаком)
	+ вынести работу с Gui в отдельный класс
	+ создать функцию для рендера
		
*viShader
	доработаем класс:
		-- сделать обертки для glUniformMatrix4fv и glGetUniformLocation (одна функция: закинул имя и данные, он все сам сделал)
	
*vuUi
	+ Базовая реализация
	- Вернуть базовые функции, в рабочее состояние
	
*viCamera
	+ добавить логику работы с ортографической проекцией
	- синхронизировать камеры ортографический и перспективной (нужно сначала переписать рендер, ибо для отладки будет рендер в 2 окна)
	- добавить наблюдение за шейдером, и изменить внутреннию работу с ним , через шейдерные функции
	- вычеслять projection * view * model  и полученный результат запихивать в шейдер
	
*ver Shaider
	- заменить projection * view * model  на одну переменную
