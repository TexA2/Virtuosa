# Virtuosa

OpenGL point cloud renderer.

## 🚀 Quick Start

### Clone repository
```bash
git clone --recursive https://github.com/TexA2/Virtuosa.git
cd Virtuosa
```

### 📦 Install dependencies (Ubuntu/Debian)
```bash
sudo apt-get install libglfw3-dev libglm-dev libpcl-dev libgtk-3-dev
```

### 🔨 Build
```bash
cmake -B build .
cd build
make
```


### Run
```bash
./main
```

## 🎮 Camera Controls

**Rotation:**
- 🖱️ Mouse drag — Pitch/Yaw (look around)
- Q / E — Roll (tilt left/right) 🔄

**Movement:**
- W / S — move forward/back (Z-axis) ⬆️⬇️
- A / D — move left/right   (X-axis) ⬅️➡️
- Z / C — move up/down      (Y-axis) Y-axis ⏫⏬

## 📚 Dependencies

- ⚡ GLFW 3.3+
- 🧮 GLM
- ☁️ PCL (Point Cloud Library)
- 🖥️ GTK3 (optional)

## Note

This is a pilot version of the project. Functionality may change.

